# Copy to .env.local (gitignored) and fill in to enable the "Upload Selfie"
# AI customization in the Customize panel. Without this, that feature
# responds with a clean "AI detection not configured" message rather than
# breaking — see analyze-face-core.mjs / vite.config.ts.
GEMINI_API_KEY=
